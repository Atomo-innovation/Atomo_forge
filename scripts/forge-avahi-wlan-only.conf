# Installed by npm run board:setup / board:fix-hosts
# Stops Docker bridges from advertising electron.local (wrong 172.x addresses).
[server]
allow-interfaces=wlo1,wlan0,eth0,enp0s31f6,enp1s0
publish-aaaa=no
